# Intermediate split layers (regenerated by split_all.py). Only the WebP in
# assets/photo and the Base.png sources are tracked.
_src/**/[0-9]*.png

# Local virtualenv + model caches.
.venv/
__pycache__/
